work/
scans/
__pycache__/
_master-*.ttf

# Generated capture sheets. The recommended "book" size is committed for both
# papers, together with every layout JSON, so anyone can print and scan
# without running Python. The bulky "normal" (portrait) PDFs are not - they
# are one command away:  python3 make_template.py --size normal
templates/handwriting-*-normal.pdf
